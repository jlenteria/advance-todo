import 'package:flutter/material.dart';

class DoneTodoScreen extends StatelessWidget {
  const DoneTodoScreen({super.key});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Text('Done Todos');
  }
}
